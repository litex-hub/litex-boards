# OpenOCD config for TrellisBoard (Lattice ECP5 LFE5UM-85F).

adapter driver ftdi
adapter speed 5000
transport select jtag
ftdi vid_pid 0x0403 0x6010
ftdi channel 0
ftdi layout_init 0xfff8 0xfffb
reset_config none

jtag newtap ecp5 tap -irlen 8 -expected-id 0x81113043
